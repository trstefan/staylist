generator client {
  provider    = "prisma-client-js"
  engineType  = "client"
}

datasource db {
  provider = "postgresql"
}

model Song {
  id          String   @id @default(uuid())
  title       String
  artist      String
  url         String?
  description String?
  createdAt   DateTime @default(now())
}
